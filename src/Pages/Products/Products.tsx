import React from "react";
import styles from "./Products.module.css";
import cx from "classnames";

const Products = (): JSX.Element => {
	return (
		<div className={styles["page-container"]}>Products Page</div>
	);
};

export default Products;