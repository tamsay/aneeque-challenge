import React from "react";
import styles from "./Orders.module.css";

const Orders = (): JSX.Element => {
	return (
		<div className={styles["page-container"]}>Orders Page</div>
	);
};

export default Orders;