import React from "react";
import styles from "./Shipment.module.css";
import cx from "classnames";

const Shipment = (): JSX.Element => {
	return (
		<div className={styles["page-container"]}>Shipment Page</div>
	);
};

export default Shipment;