import React from "react";
import styles from "./Settings.module.css";

const Settings = (): JSX.Element => {
	return (
		<div className={styles["page-container"]}>Settings Page</div>
	);
};

export default Settings;